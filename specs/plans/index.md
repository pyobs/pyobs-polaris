# Plans

Implementation plans, checklist-style.

None yet — every design doc migrated out of `DEVELOPMENT.md` into `specs/design/` was already
shipped and closed by the time this split happened. What's planned next lives in `TODO.md` (repo
root), not here; a TODO item graduates into its own `specs/plans/*.md` once someone starts
designing it in enough detail to warrant one, following the pattern in `specs/design/`.
